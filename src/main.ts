import 'dotenv/config';

async function main() {
  console.log('nerdyAds — Ad Generation Engine');
  console.log('================================');
  // Pipeline entry point — will be implemented in Phase 6
}

main().catch(console.error);
