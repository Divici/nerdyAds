# nerdyAds — Claude Code Project Instructions

## What This Project Is
An autonomous ad generation engine for Varsity Tutors (Nerdy) SAT test prep campaigns on Facebook/Instagram. It generates ad copy, evaluates quality across 5 dimensions, iterates to improve weak ads, and tracks performance per token spent.

## Current Status
- **Phase:** Phase 9 complete (Evals suite). Phase 10 (Demo UI) is next.
- **Key docs to read first:**
  - `docs/implementation-plan.md` — 12-phase build plan with Phases 8.5 and 8.7 added
  - `docs/decision-log.md` — 30 decisions with full rationale (D-030 covers Langfuse integration)
  - `data/reference/competitor_ads.json` — 65+ ads (41 Varsity Tutors + Princeton Review, Chegg, Kaplan) with longevity data

## Reference Ads — Now Available (partially resolves D-018)
We now have 41 real Varsity Tutors ads from Meta Ad Library with longevity data (days active). This partially resolves D-018 (no first-party reference ads). Key points:
- **VT ads available:** 41 ads, longest-running 10 days (GPA-SAT disconnect messaging is their top performer)
- **Competitor ads with longevity:** Kaplan (35 days longest), Princeton Review (20 days), Chegg (limited)
- **Phase 8.5** builds a tiered reference set (~15-18 ads) from these real ads to validate the evaluator
- Tiers are **copy-tier** (v1 scope) — visual quality not yet evaluated
- Schema supports v2 extension with `creative_metadata` fields for image evaluation
- Still no real performance metrics (CTR, conversions) — longevity is our best proxy
- System remains designed to recalibrate when/if real performance data arrives

## Tech Stack
- **Language:** TypeScript (full-stack — pipeline + UI)
- **Generation model:** Gemini Flash via `@google/genai`
- **Evaluation model:** Gemini Pro via `@google/genai`
- **Observability:** Langfuse (cost tracking, traces)
- **Testing:** Vitest (unit + integration + evals)
- **Frontend:** Vite + React + Recharts + Tailwind (read-only viewer)
- **Validation:** Zod for LLM output parsing

## Architecture
4 specialized agents connected through an orchestrator:
- **Researcher** (Pro) — extracts patterns from competitor ads
- **Writer** (Flash) — generates ad batches from briefs
- **Editor** (Flash) — targeted improvement of weak dimensions
- **Evaluator** (Pro) — scores 5 dimensions with rationales + confidence

Pipeline flow: Brief → Writer generates batch of 5-8 → Evaluator scores → Pass (≥7.0) → Library | Fail → Editor improves → Re-evaluate → Max 3 cycles → Accept or reject

## Key Design Decisions (summary)
- Evaluator-led architecture (build evaluator first)
- Layered context (each agent sees only what it needs)
- Seed + snapshot for reproducibility
- Max 3 regeneration cycles per ad
- Quality ratchet: threshold = max(7.0, running_avg - 0.5)
- Confidence: self-reported + rule-based hybrid
- Rate limiter: 100ms min delay, 5 max concurrent, 3 retries with backoff
- 10 briefs × 5-8 ads each for 50+ total
- JSON only output (no CSV), UI handles visualization

## 5 Quality Dimensions (weighted)
1. Clarity — 25%
2. Value Proposition — 25%
3. Emotional Resonance — 20%
4. Call to Action — 15%
5. Brand Voice — 15%

## Project Structure (target)
```
src/
  agents/        # researcher.ts, writer.ts, editor.ts, evaluator.ts
  pipeline/      # orchestrator.ts, iteration-loop.ts, batch-runner.ts
  evaluate/      # scoring.ts, threshold.ts, failure-taxonomy.ts
  metrics/       # tracker.ts, cost.ts
  types/         # ad.ts, brief.ts, evaluation.ts, pipeline.ts, patterns.ts
  config/        # weights.ts, thresholds.ts, models.ts, prompts.ts
  utils/         # gemini-client.ts, rate-limiter.ts, snapshot.ts, langfuse.ts, logger.ts, hash.ts
ui/              # Vite + React frontend
data/
  reference/     # competitor_ads.json, patterns.json (generated by researcher)
  calibration/   # strong.json, weak.json, borderline.json
  output/        # generated ads, evaluations, metrics per run
  briefs.json    # 10 briefs
docs/
  decision-log.md
  implementation-plan.md
  limitations.md
  technical-writeup.md
tests/
  unit/
  integration/
  evals/         # API-hitting eval tests (run separately via npm run eval)
```

## Build Order
Phase 1: Scaffold → Phase 2: Types + Config → Phase 3: Utilities → Phase 4: Evaluator (CRITICAL) → Phase 5: Writer/Editor/Researcher → Phase 6: Pipeline → Phase 7: Calibration run → Phase 8: Full 50+ ad run → Phase 9: Evals → Phase 10: UI → Phase 11: Docs → Phase 12: Polish

## Testing Approach
- TDD — write tests first for all pure logic
- Unit tests: scoring math, thresholds, confidence, taxonomy, rate limiter, cost
- Integration tests: mocked Gemini responses, full pipeline scenarios
- Evals (separate): calibration ranking, consistency, dimension independence, improvement verification, regression check
- Target: 25+ tests total

## Environment
- API key in `.env` (GEMINI_API_KEY)
- `.env` is gitignored
- STUDY_GUIDE.md is gitignored (personal study reference)

## User Preferences
- TDD approach for all building
- Wants honest documentation of failures/limitations
- Strong frontend skills — UI will match Varsity Tutors website styling (polish phase)
- Has 2 full 18-hour days
- Update STUDY_GUIDE.md after architectural decisions and major implementations
- Update docs/decision-log.md during build with observations, failed approaches, post-run analysis
